var heeyhomeMyorder = {
	/*
	 * 初始化
	 */
	init : function() {
		heeyhomeMyorder.arrowChange();
	},
	/*
	 * 箭头切换效果
	 */
	arrowChange : function(){
		var _arrow = $(".order_title div").eq(6);//全部后面的小三角形
		var _area = $(".order_content .block .size p");//建筑面积里面的小三角形
		var _arrowcnt = $(".order_title div").eq(6).find("ul");//全部后面的小三角形里的内容
		var _areacnt = $("")
	}
}
